name: MCSync
main: com.mcsync.plugin.mcsync
version: '1.2.0'
load: STARTUP
author: MCSync
website : https://mcsync.live
softdepend:
    - LuckPerms
commands:
    mcsync:
        usage: /<command> [help | get | set | reload]
        aliases: [mcs]
        description: Command for MCSync.
        permission: mcsync.admin